# Build artifacts
target/
**/*.rs.bk

# Anchor build outputs
.anchor/
programs/**/.so
programs/**/keypair.json

# Test fixtures — pinned external .so binaries dumped on demand
# (see programs/aargau-manager/tests/fixtures/mod.rs for dump instructions).
programs/aargau-manager/tests/fixtures/*.so

# Solana keypairs (never commit)
**/*-keypair.json
deploy-keypair.json
wallet.json

# IDL (generated — commit only if publishing on-chain)
# Uncomment to ignore: target/idl/

# Node / TypeScript client (if added later)
node_modules/
dist/

# Environment
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Internal agent instructions (never expose in public repo)
AGENTS.md
CLAUDE.md
